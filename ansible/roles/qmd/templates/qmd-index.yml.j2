collections:
  workspace:
    path: {{ item.workspace_dir }}
    pattern: "**/*.{md,txt,csv}"
{% for col in qmd_extra_collections | default([]) %}
  {{ col.name }}:
    path: {{ item.workspace_dir }}/{{ col.path }}
    pattern: "{{ col.pattern }}"
{% endfor %}
